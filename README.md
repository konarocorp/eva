# Eva

A single-file Python toolbox for Linux system scripting. Zero dependencies.

Simply download `eva.py` and add `import eva` to your code.

Python 3.13 or higher is required. It is developed under this version.

You can find the full documentation [here](https://konarocorp.github.io/eva/en).
